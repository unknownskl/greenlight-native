{
  "name": "greenlight-client-poc",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "DEBUG=* node dist/bin/app.js",
    "dev": "DEBUG=* node dist/index.js",
    "prod": "tsc --build && echo \"Creating packed binary...\" && webpack --config webpack.config.js && echo \"JS Binary created: dist/app.js\"",
    "clean": "rm -rf dist/ && rm -rf *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^18.7.23",
    "greenlight-xcloud": "^0.1.0",
    "ts-loader": "^9.4.1",
    "typescript": "^4.9.3",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
