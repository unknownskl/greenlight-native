{
  "name": "greenlight-xcloud",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist/ && rm -rf *.tsbuildinfo",
    "test": "mocha"
  },
  "dependencies": {
    "debug": "^4.3.4"
  },
  "devDependencies": {
    "@types/debug": "^4.1.7",
    "@types/mocha": "^10",
    "@types/node": "^18.7.23",
    "mocha": "^10.0.0",
    "typescript": "^4.8.4"
  }
}
