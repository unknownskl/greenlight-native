{
  "private": true,
  "scripts": {
    "start": "cd client-poc && yarn start",
    "build": "yarn workspaces foreach -v run build && yarn workspace greenlight-client-poc prod",
    "clean": "yarn workspaces foreach -v run clean",
    "test": "yarn workspaces foreach -v run test"
  },
  "workspaces": [
    "packages/*",
    "client-poc"
  ],
  "packageManager": "yarn@3.2.3"
}
